class Database {
  tableName: string
  tableInfo: string | undefined
  tableFile: string | undefined
  constructor(tableName: string) {
    this.tableName = tableName
  }
  // 加载 db info
  // 加载 db file
  // 获取 db 完整信息
  // 更新 table 内容
  // 新增 item
}